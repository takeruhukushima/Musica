# Musica 要件定義書

版：0.4 ／ 更新日：2026-09-14 ／ 状態：実装前

本書はAstro、AT Protocolによる複数ユーザー対応、および「MusicXMLを原本としてblobに保存し、アプリ側で描画・再生する」という合意に基づく。0.4では、入力形式と保存形式の分離、再生の位置づけの明確化、隔離実行の適用範囲の限定を行う。API形状、上限値、実行環境の内部構成は実装提案として記載し、技術検証後に確定する。

## 1. 目的

**Musicaは、楽譜を本人のAT Protocolアカウントで公開し、Webで表示・確認再生し、編集用ソースごと再編集できる場所である。**

プロジェクト作者と一般利用者に同じ作品管理機能を提供する。互換性のあるAT Protocolアカウントでログインし、自分のPDS（Personal Data Server）へ作品を保存する。運営者のアカウントへ全員の作品を集約しない。

原本はMusicXMLとする。楽譜の内容を宣言的なデータとして保存し、描画と再生はアプリ側の信頼されたコードが行う。原本の実行を閲覧の必須経路に置かない。

### 設計原則

以降のすべての判断の上位に置く4つの原則を定める。

1. **原本は交換可能なデータである。** レコードとblobだけを取得した第三者が、Musicaの実装を参照せずに楽譜の内容を復元できる。
2. **Musicaは唯一のAppViewではない。** レコードは自己記述的であり、描画エンジン・フォント・レイアウトを指定しない。別実装が別の見た目で同じ作品を表示することを妨げない。
3. **実行可能コードを必須経路に置かない。** 任意コードの実行を伴う機能は、それが無くても作品の公開・表示・再生が成立する位置にのみ置く。
4. **書かれていない音を鳴らさない。** 記譜内容から音楽的判断を自動で補完しない。Musicaは解釈者ではなく、記譜の確認手段である。

原則2の帰結として、Musicaは利用者を囲い込まない。原則4の帰結として、Musicaは作者の意図を上書きしない。いずれもAT Protocolと記譜という選択の目的そのものであり、失ってよい性質ではない。

### 0.3からの変更

| 項目 | 0.4の方針 |
| --- | --- |
| 入力形式 | MusicXMLに加えABCを受理。取り込み時にMusicXMLへ変換する |
| 保存形式 | 原本は常にMusicXML。変更なし |
| `generator` | `editSource` へ一般化。`.ts` 生成器に限らず「作者が編集する実体」を指す |
| ABC経路 | コード実行を伴わないため隔離実行が不要。第1段階に含める |
| コードシンボル | 音へ自動変換しないことを明文化（設計原則4） |
| 再生の位置づけ | 「演奏の再現」ではなく「記譜の確認」であることを明文化し、限界を仕様に書く |
| 隔離実行 | 「他者のコードを閲覧者の環境で実行する」場合に限定。作者自身の生成器利用には不要 |
| 取り込み画面 | テキスト貼り付けと即時プレビューの2ペインを初期版に含める |
| エージェント向け | CLIより先にMCPサーバを提供する |
| 実装順序 | 認証より前に「貼る→鳴る」を完成させ、早期から自己利用できる状態を作る |

## 2. 成功条件とスコープ

### 初期版の完成条件

- 作者以外の利用者が、許可リストやコード変更なしでログイン・公開・更新・削除できる。
- MusicXMLまたはABCと作品情報だけで公開できる。
- 公開直後に別端末から開いても、サイト再ビルドなしで描画できる。
- 原本をダウンロードして外部の楽譜ソフト・エディタ・AIで修正し、再公開できる。
- ABCで投稿した作品を取得すると、投稿したABCがそのまま返る。
- 代表譜例が譜面表示と確認再生の両方を実現し、再生位置が譜面上で示される。
- 記譜された和音が、書かれた通りの構成音・音域で鳴る。書かれていない和音が鳴らない。
- Musicaのコードを使わない別実装が、公開レコードとblobだけから同じ作品を描画できる。
- 悪意あるXML・圧縮ファイルを取り込んでも、解析側が停止・過負荷・情報漏洩を起こさない。

### 初期版の対象外

本格的なGUI楽譜エディタ、他者コードの閲覧時実行（第4段階）、演奏表現データ、共同編集、版履歴の完全保存、全体検索・新着フィード、限定公開・非公開クラウド同期、課金・販売、MIDI書き出し、音声ファイル生成、複数音色・移調・パート抽出、MusicXMLの全機能への対応保証、コードからの自動採譜を対象外とする。

## 3. 用語と保存対象

| 用語 | 役割 |
| --- | --- |
| 原本 | 作品の内容を定める MusicXML。唯一の正本 |
| 編集用ソース | 作者が実際に編集している実体。ABC または `.ts` 生成器。任意。正本ではない |
| 描画エンジン | MusicXMLを譜面SVGへ変換する共通ライブラリ |
| Bravura | 音楽記号の形を提供する共通フォント |
| Lexicon | 作品レコードの型と制約を定義する仕様 |
| レコード | タイトル、原本への参照、形式識別子などの作品情報 |
| blob | PDSに保存する原本・編集用ソースのファイル本体 |
| 派生物 | SVG、PDF、MIDI、共有用画像など。原本の代わりにはしない |

描画エンジンとBravura本体はアプリが提供し、各blobへ複製しない。原本はテキスト（またはその圧縮）として取得でき、外部の楽譜ソフトとAIの双方が解析・編集できる。更新時は新しいblobをアップロードして同じ作品レコードの参照を更新する。[blob公式仕様](https://atproto.com/specs/blob)

## 4. 利用者と権限

| 利用者 | 権限 |
| --- | --- |
| 未ログイン | 公開作品・指定作者の一覧・原本閲覧、原本取得、譜面表示、確認再生、取り込み画面での試用 |
| ログイン利用者 | 上記に加え、端末内下書き、自分の作品の公開・更新・削除、Bluesky共有 |
| プロジェクト作者 | 一般利用者と同じ作品作成・管理機能 |
| 運営者 | Musicaでの表示除外、障害対応、キャッシュ管理。他人のPDSを編集しない |

所有者はリポジトリのDIDで判定する。ハンドル変更後も同一所有者として扱い、PDS移行時は現在の所在を再解決する。作品内の作曲者・編曲者名を認証上の所有者に使わない。

アカウント切替時はセッション・下書き・編集中の対象を分離する。他人の作品は原本を取得できても、そのレコードを上書きできない。派生作品を公開する場合は本人のリポジトリに新しい作品を作る。

**取り込み画面はログインなしで使える。** 貼り付け・描画・確認再生までを未ログインで提供し、公開操作でのみログインを求める。

## 5. 制作・公開・閲覧フロー

### 制作から公開

1. 人、既存の楽譜ソフト、AI、または手元の生成器がMusicXMLまたはABCを用意する。
2. 取り込み画面にファイルをドラッグするか、テキストを貼る。ログインは不要。
3. 形式、容量、XML構造、記譜内容の対応範囲を検証する。§6の拒否条件に該当するものは取り込まない。
4. ABCの場合はMusicXMLへ変換し、ABC原文を編集用ソースとして保持する。
5. 譜面を即時描画し、確認再生できる。修正と再描画をこの画面で反復する。
6. ログインし、作品情報を確認して公開操作を行う。原本blob（および編集用ソースblob）を本人のPDSへ送る。
7. それらを参照する作品レコードを作成する。成功時に作品URLを示す。
8. 利用者が選んだ場合に限り、別操作でBlueskyへ共有する。

公開経路はWebとする。外部ソフトからのエクスポートをそのまま受け取れることを重視し、Musica専用の中間形式を公開の必須条件にしない。AIサービスのAPIキーも必須にしない。

### 閲覧

作品レコードを取得し、形式識別子と原本blobを確認する。原本を取得し、検証してから描画する。署名やCIDは出所・内容の確認に用い、内容が安全であることの証明には使わない。公開者の事前検証に依存せず、閲覧側でも検証する。

原本・編集用ソースの表示はテキストとして行い、HTMLとして解釈しない。原本に含まれるタイトル・作曲者名・歌詞・注釈は、画面表示時に必ずテキストとして扱う。描画成功後に譜面を表示する。取得・解析・描画に失敗しても、作品情報と原本取得の経路は残す。

### 更新・削除

同一作品の更新はレコードキーを維持する。変更前CIDによる競合検出を行い、他端末の変更を黙って上書きしない。blobのアップロード後にレコード更新が失敗した場合も、ローカル原本を保持する。

編集用ソースを持つ作品の更新は、編集用ソースを起点とする。原本は編集用ソースから再生成し、両方のblobを同時に差し替える。編集用ソースだけが更新されて原本が古いままの状態を作らない。

削除は本人の作品レコードを対象とし、アプリの表示とキャッシュを失効させる。PDSや第三者に残る複製、既存のBluesky投稿、他のAppViewのキャッシュまで一括削除できるとは扱わない。過去のblobを参照し続けない限り旧版が保存されるとは保証しないため、版履歴は初期版の対象外とする。

## 6. 原本と入力形式

### 保存形式

原本は常にMusicXMLとする。入力形式が何であっても、`source` に入るのはMusicXMLである。

- MusicXML 4.0を基準とし、下位版の文書も受理する。
- 非圧縮は UTF-8 の `.musicxml` ／ `.xml`、MIME type は `application/vnd.recordare.musicxml+xml` を基本とする。
- 圧縮は `.mxl`、MIME type は `application/vnd.recordare.musicxml` を基本とする。
- 実際に記録するMIME typeは対象PDSの受理条件を検証して確定する。拡張子や自己申告MIMEだけを信頼せず、内容をアプリで確認する。

### 受理する入力形式

| 入力 | 扱い |
| --- | --- |
| MusicXML（非圧縮） | そのまま原本とする |
| `.mxl`（圧縮） | 検証のうえ原本とする。展開して内部XMLも検証する |
| ABC | MusicXMLへ変換して原本とし、ABC原文を編集用ソースとして保存する |

ABCを受理する理由は、人とAIの双方が短く正確に書ける記法であり、コード実行を伴わないためである。ABC→MusicXML変換は純粋なデータ変換であり、隔離実行を必要としない。

**変換器は第1段階の検証項目とする。** 採用予定の描画エンジンVerovioはMusicXMLを入力に取れるがMusicXMLを出力できないため、変換器は別途必要になる。`abc2xml`（Wim Vree）系を候補とする。変換の再現性・記譜の欠落・エラー報告が実用水準に達しない場合、**ABCの受理を初期版で見送る**。ABCを描画のみに使い保存できない中途半端な状態は作らない。

### 拒否条件

原本の解析は、信頼できない入力の解析として扱う。以下を検出した時点で取り込みを中止し、理由を表示する。

- `DOCTYPE` 宣言および実体（entity）定義を含む文書。外部実体参照を一切解決しない。
- 展開後サイズ、実体展開回数、ネスト深さが上限を超える文書。
- `.mxl` 内のエントリ名に絶対パス・親ディレクトリ参照を含むもの。
- `.mxl` の展開後合計サイズ、エントリ数、圧縮率が上限を超えるもの。
- `.mxl` の `META-INF/container.xml` が存在しない、または指す原本パスが書庫内に収まらないもの。container.xml自体も本節の拒否条件で検証する。
- 整形式でないXML、宣言された文字符号化と実体が一致しないもの。
- 描画エンジンが致命的エラーを返すもの。

外部DTD・スキーマ・画像・リンク先をネットワークから取得しない。原本に含まれる外部参照は解決せず、無視するか拒否する。ABCについても、入力長・声部数・小節数の上限と、変換処理の時間制限を設ける。

### 対応記譜の範囲

初期版は五線譜の一般的な記譜を対象とする。少なくとも大譜表、複数パート、複数声部、和音、休符、臨時記号、調号、拍子、タイ、スラー、連桁、三連符、反復記号、コードシンボル、歌詞を表示できることを条件とする。

**同時に鳴る音の構成・数・音域に、Musica独自の制限を設けない。** 記譜された和音は書かれた通りに表示・再生する。

対応外の記譜を含む原本は、可能な範囲で描画し、未対応要素があることを表示する。取り込み自体は拒否しない。MusicXMLの全機能への対応は保証しない。

### 再現性

描画エンジン、フォント、レイアウト設定の版をアプリが管理し、レコードには記録しない。原本はレイアウトを規定せず、描画結果がAppViewごとに異なることを正常な状態として扱う。Musicaは自身の描画設定を公開し、同じ見た目を再現したい実装が追随できるようにする。

## 7. 描画

### 採用方針

描画エンジンは **Verovio** を第一候補とする。

- MusicXMLを直接入力に取り、SVGを出力する。中間形式を自作しない。
- **Bravuraを同梱しており**、`font` オプションで明示指定できる。0.2で決めたフォント選択を維持できる。
- MIDI出力と時刻問い合わせAPIを同じエンジンが提供するため、§8の確認再生とハイライトを別実装なしで満たせる。
- MEI、ABC、Humdrum等も入力に取れるため、将来の受理形式追加が実装の作り直しにならない。

代替候補は **OpenSheetMusicDisplay（OSMD）** とする。MusicXMLをVexFlowで描画するライブラリで、BSD-3-Clauseであり、VexFlow + Bravuraの構成をそのまま使える。VerovioがWebAssemblyであることによるロード量、およびGPL-3.0／LGPL-3.0というライセンス条件が対象環境で問題になる場合はこちらへ切り替える。

**描画エンジンの選択はレコード形式に影響しない。** 原本がMusicXMLである限り、エンジンの差し替えは保存済みデータを壊さない。この可逆性を理由に、第1段階での実測をもって確定する。

### 出力の扱い

描画エンジンが出力するSVGは信頼された変換結果として扱うが、原本由来のテキスト（タイトル、作曲者、歌詞、注釈、コードシンボル）がSVGへ混入するため、以下を必須とする。

- SVGを親画面へ挿入する前に、スクリプト要素、イベント属性、外部参照を除去する。
- 原本由来のテキストは要素のテキストとして設定し、マークアップとして解釈しない。
- ページ数、SVGサイズ、要素数が上限を超える場合は描画を中止する。

SVGの書き出しを画面から提供し、PDFはブラウザ印刷で取得可能にする。これらの派生物はPDS保存の必須条件ではなく、レコードの必須スキーマにも含めない。

## 8. 確認再生

### 位置づけ

Musicaの再生は、**記譜された音高と音価を耳で確認するための手段**である。演奏の再現ではない。画面上でもこの位置づけを明示し、「再生」ではなく確認用途であることが利用者に伝わる表示とする。

### 鳴らすもの、鳴らさないもの

| 対象 | 扱い |
| --- | --- |
| 記譜された音符・和音 | 書かれた通りに鳴らす。構成音・音域・声部数を変更しない |
| 記譜された休符・タイ・反復 | 記譜通りに反映する |
| コードシンボル（`<harmony>`） | **表示のみ。音に変換しない** |
| 記譜されていない伴奏・ベース・内声 | **生成しない** |
| 調や進行からの補完 | **行わない** |

設計原則4の具体化である。コードシンボルから伴奏を自動生成する機能（他ツールにおける自動伴奏やコード実体化に相当するもの）は、初期版でも後続でも実装しない。作者が鳴らしたい和音は作者が記譜する。

### 方式

1. 描画エンジンのMIDI出力機能で、原本から演奏データを生成する。
2. 生成された演奏データを解析し、Web Audioで再生する。音源はアプリ共通のピアノ1音色とし、再生操作時にロードする。
3. 再生中は、描画エンジンの時刻問い合わせAPIで現在発音中の要素を取得し、SVG上の該当要素へ強調用のクラスを付与する。

演奏イベントの内部表現は以下とする。これは音声再生へ渡す内部APIであり、保存形式ではない。

| 項目 | 内容 |
| --- | --- |
| `startMs` / `durationMs` | 発音開始と長さ |
| `midiNote` | 0〜127の音高 |
| `velocity` | 発音強度 |
| `elementId` | 対応する譜面要素の識別子。ハイライト用 |

### 表現の限界

以下は初期版で再現しない。仕様上の制約として明記し、利用者に期待させない。

- スイング、その他のテンポの揺れ、アゴーギク
- タッチ、アーティキュレーションの機微、微細なダイナミクス
- ペダル、音色変化、複数音色
- 装飾音・トリル等の演奏上の解釈差

MusicXML自体がこれらの多くを十分に表現できないため、これはMusicaの実装上の都合ではなく形式の性質である。ジャズをはじめ、演奏表現が本質である音楽の「演奏」を再現する用途には使えない。**和音の構成音や声部配置が意図通りかを確かめる用途には十分機能する。**

演奏表現を別レイヤのデータとして後から追加する余地は残すが、初期版の対象外とし、追加時も原本の正本性を変更しない。

### 範囲

初期版はピアノ1音色、再生・一時停止・停止・シーク・音量、および再生位置のハイライトを提供する。複数音色、テンポ変更の精緻化、繰り返しの展開精度向上、MIDIファイル書き出し、メトロノームは後続拡張とする。再生に適さない原本は、その旨を表示する。

## 9. AT ProtocolとLexicon

独自トップレベルコレクションは楽譜作品用の1種類から始める。説明用の仮NSIDは `com.example.musica.score`。本番では管理するドメインに基づき正式なNSIDを公開する。**第三者AppViewはこのコレクション名を決め打ちで参照するため、長期に保持できるドメインを選び、公開後に変更しない。**

### レコード案

| フィールド案 | 必須 | 内容 |
| --- | --- | --- |
| `$type` | 必須 | 正式な楽譜レコードNSID |
| `schemaVersion` | 必須 | レコード構造の版 |
| `title` | 必須 | 作品名 |
| `description` | 任意 | プレーンテキストの説明 |
| `composer` / `arranger` / `lyricist` | 任意 | 作曲者・編曲者・作詞者 |
| `tags` | 任意 | 分類用タグ |
| `source` | 必須 | 原本のLexicon blob型参照。CID・MIME type・sizeを含む |
| `sourceFormat` | 必須 | `musicxml` ／ `mxl`。慣習に頼らず明示する |
| `sourceName` | 必須 | ファイル名。パスとして実行・展開しない |
| `editSource` | 任意 | 編集用ソースのblob参照 |
| `editFormat` | 条件付き必須 | `editSource` がある場合に必須。`abc` ／ `typescript` |
| `editRuntime` | 条件付き必須 | `editFormat` が `typescript` の場合に必須。固定依存とAPI契約を指す識別子 |
| `parts` | 任意 | パート名の配列。一覧表示用の要約 |
| `durationSec` | 任意 | 概算演奏時間。一覧表示用の要約 |
| `createdAt` / `updatedAt` | 必須 | 公開日時・更新日時 |
| `rights` | 任意 | 利用条件・ライセンス |
| `thumbnail` | 任意 | 共有用PNG/JPEGのblob参照 |

`parts` と `durationSec` は原本から導ける冗長な情報だが、一覧画面が全作品のblobを取得せずに済むよう記録する。表示用の要約であり、原本と食い違う場合は原本を正とする。

### 編集用ソースの扱い

`editSource` は**作者の再編集のための付属物**であり、表示・再生の経路に入らない。

- `editSource` が無い作品、解釈できない作品、取得に失敗した作品も、`source` から通常どおり表示・再生できる。
- `editSource` の内容で `source` を自動的に置き換えない。利用者が確認して更新操作を行う。
- `editFormat` が `typescript` の場合、その実行は第4段階の機能である。実行できない環境でも作品は完全に閲覧できる。

### 自己記述性

レコード単体から、原本の形式・所在・意味が判別できることを必須とする。Musicaの実装知識を前提とするフィールド、暗黙の既定値、Musica固有の識別子で内容が変わるフィールドを置かない。`editSource` を解釈できない実装でも、`source` だけで完全に描画できる。

### 公開手順

blobを先にアップロードし、その参照を持つレコードを作成する。レコード作成完了まで公開成功と扱わない。試行ごとのレコードキーを保持し、タイムアウト時は同じキーを照会して重複作成を避ける。派生SVG・PDF・音声のblob参照は必須スキーマに含めない。

OAuthには `@atproto/oauth-client-browser` を採用する。作品コレクションと必要なblob操作を許可範囲とし、Bluesky投稿権限は共有機能に合わせて扱う。アカウントのパスワードを原本や解析処理へ渡さない。[OAuth公式](https://atproto.com/guides/about-oauth)

作品識別子は `at://{DID}/{collection}/{rkey}` とする。Blueskyへの投稿は別操作とし、文章・作品URL・任意の譜面画像を確認してから送る。共有投稿の失敗で公開済み作品を削除しない。

## 10. `.ts` 生成器と隔離実行の適用範囲

### 隔離実行が必要な場面は1つだけ

隔離実行が必要なのは、**他者が書いたコードを閲覧者の環境で実行する**場合に限る。それ以外の経路には不要である。

| 場面 | 隔離実行 | 段階 |
| --- | --- | --- |
| 作者が手元で生成器を実行し、出力したMusicXMLを投稿する | **不要** | 第1段階から可能 |
| ABCを投稿し、アプリがMusicXMLへ変換する | **不要**（データ変換） | 第1段階 |
| 作者が自分の生成器をアプリ上で実行する | 必要 | 第4段階 |
| 閲覧者が他者の生成器を実行して譜面を得る | 必要 | 第4段階 |

**作者がプログラムで楽譜を作ること自体は、Musicaの機能を必要としない。** 手元で生成器を走らせてMusicXMLを出力し、それを通常の取り込み導線に通せばよい。アプリはその原本が手書きか楽譜ソフト由来か生成器由来かを区別しないし、区別する必要もない。第4段階は「他人の生成器を動かす」ための機能であり、個人の創作には不要である。

### 生成器の規約

`editFormat` が `typescript` の場合、以下を満たす。

- 出力契約は**MusicXML**とする。SVGや描画命令ではない。
- 単一のTypeScript ES module、UTF-8とする。
- 使用可能なimportはMusicaが提供する型・補助APIに限る。任意URL import、動的import、Node.js APIを許可しない。
- `editRuntime` をレコードへ記録し、非対応版には明確なエラーを表示する。最新版へ暗黙に置換しない。
- 出力は§6の検証をそのまま通す。生成器の出力を検証済みとして扱わない。

出力をMusicXMLに限定することで、出力検証はスキーマ検証に還元される。SVGサニタイズ、描画アダプター、親DOM保護のための特別な仕組みを生成器側に用意しない。

### 隔離実行の必須条件（第4段階）

アプリ上での生成器実行を公開する場合に限り、以下を満たす。満たせない場合はこの機能を公開しない。作品の公開・表示・再生はこの条件に依存しない。

- ログイン済みアプリのウィンドウやサーバープロセスで生成器を直接実行しない。
- 生成器の実行環境へOAuthトークン、Cookie、IndexedDBの下書き、PDSクライアントを渡さない。
- 外部通信、親DOM、ポップアップ、ナビゲーション、ストレージへのアクセスを制限する。import制限や型検査だけを隔離の代用にしない。
- 無限ループ・過大出力に対して停止可能な実行単位を用い、時間・出力サイズを制限する。
- コンパイルも資源制限の対象とし、利用者由来のコンパイラプラグインを実行しない。
- CLIでも生成器をNode.jsの権限でimport・実行しない。

生成器はDOMを必要としないため、描画環境をWorker内に用意する技術検証は不要である。

## 11. Astroと実行環境の構成

| 部分 | 役割 |
| --- | --- |
| Astro静的Web | ホーム、作者一覧、取り込み・作品・管理画面の枠、認証と公開操作 |
| ブラウザのAT Protocolクライアント | DID解決、レコード取得・保存・更新 |
| 原本配信プロキシ／CDN | 公開レコードに紐づくblobを取得・検証し、配信 |
| 入力検証処理 | §6の形式・拒否条件の検査。必要時ロード |
| ABC変換処理 | ABC→MusicXML変換。必要時ロード |
| 描画処理 | 描画エンジンとフォントのロード、SVG生成と無害化 |
| 確認再生処理 | 演奏データの生成、アプリ共通音源による再生、ハイライト |
| MCPサーバ／CLI | 検証、描画、構造要約、ローカルプレビュー、公開 |
| 隔離ランナー（第4段階） | 他者の生成器を実行し、MusicXMLを生成 |

blobをブラウザのscript URLとして直接実行しない。PDS blob配信には必要なプロキシ／CDNを設ける。Honoは必須ではなく、Astroの動的エンドポイント等も選択可能とする。[blob配信仕様](https://atproto.com/specs/blob)

作品データは閲覧時に取得する。初期URL案は `/score/?uri=...` と `/profile/?did=...`。新規投稿や原本更新のたびにAstroを再ビルドしない。作品別OGP・SSR・全体検索は後続機能とする。

サーバー側で原本を解析する場合も、§6の拒否条件とリソース制限を同一に適用する。XML解析を行うライブラリは外部実体解決を既定で無効化した設定で使う。

## 12. 画面

| 画面 | 内容 |
| --- | --- |
| ホーム | 使い方、取り込み画面への導線、ログイン、作品URL・作者への入口 |
| 取り込み・公開 | 2ペイン編集、検証結果、譜面プレビュー、確認再生、メタデータ、公開操作 |
| マイ楽譜 | 本人の公開作品と端末内下書き |
| 作品詳細 | 作者・説明、譜面表示、確認再生とハイライト、原本・編集用ソースの表示と取得、本人のみ編集・削除 |
| 作者ページ | 指定DIDの公開作品をページング表示 |
| 設定・認証復帰 | セッション管理、下書きの書き出し・削除、エラー案内 |

### 取り込み画面

左にテキスト入力、右に譜面と再生操作を置く2ペインとする。入力が変わるたびに再描画し、貼った直後に譜面と音で確認できる状態を初期版に含める。

- ファイルのドラッグとテキスト貼り付けの両方を受け付ける。
- 貼られた内容の形式（MusicXML / ABC）を自動判別し、判別結果を表示する。
- ログインなしで描画・確認再生まで到達できる。公開操作でのみログインを求める。

これは楽譜を図形的に編集するエディタではなくテキスト入力欄であり、GUI楽譜エディタを導入しない方針と矛盾しない。

### エラー表示

エラーは**音楽の言葉で**返す。XMLの行番号や内部要素名を主たる表示にしない。

```
✕ Line 847: unexpected element <backup>
○ 12小節目が4拍に足りません（現在3.5拍）
```

内部情報は詳細表示として折りたたむ。

### 編集

原本の編集は初期版では外部の楽譜ソフト、エディタ、AI CLIを基本とする。下書きは原本・編集用ソースのバイト列と作品情報を端末内に保存し、ファイルとして持ち出せるようにする。

## 13. エージェント向けインターフェース

人間だけでなくAIエージェントが作品を作ることを想定する。**エージェントは生成した音楽を聴けない**ため、読み取れる形の応答を返すことを設計目標とする。

### MCPサーバを先に提供する

CLIより先にMCPサーバを提供する。エージェントがファイル出力とコマンド実行を往復せずに、検証・描画・要約・公開を直接呼べる状態を優先する。CLIはその薄いラッパーとして後から用意する。

| 操作 | 応答 |
| --- | --- |
| `render` | 譜面画像。エージェントが視覚的に確認できる |
| `check` | 構造要約と警告のテキスト |
| `play` | 人間向けのローカルプレビュー |
| `publish` | 本人のPDSへの公開（後続） |

### `check` の応答

合否だけを返さない。エージェントが自己修正できる情報を返す。

```
16小節 / 4/4 / Bb / 2パート
旋律 音域 D4–A5 / 跳躍最大 9度 (m.5)
和音 4箇所 / 最大 4声
コードシンボル: | Bb | G7 | Cm7 | F7 | Bb | G7 | Cm7 F7 | Bb |
警告: m.12 拍数不足 (3.5/4)
警告: m.7 の 10度跳躍は歌唱が困難な可能性
```

コードシンボルは構造の要約として報告するが、§8の通り音には変換しない。

## 14. 機能要件と受け入れ基準

| ID | 要件／試験 | 合格条件 |
| --- | --- | --- |
| AC-01 | 第三者のログイン・公開 | 作者用設定を流用せず、本人のPDSへblobと作品レコードを保存できる |
| AC-02 | アカウント分離 | BがAの作品を更新・削除できず、下書きも混在しない |
| AC-03 | 単一原本の公開 | MusicXMLまたはABCと作品情報だけで公開でき、SVG・音声の事前添付を要求しない |
| AC-04 | 公開直後の別端末閲覧 | サイト再ビルドなしで取得・描画できる |
| AC-05 | 原本の保存精度 | ダウンロードした原本がアップロード原本とバイト単位で一致する |
| AC-06 | 更新 | 更新後も作品URLを維持し、新しい原本で再描画できる |
| AC-07 | 代表譜例 | 大譜表、複数パート、複数声部、和音、休符、臨時記号、タイ、三連符、コードシンボル、歌詞を表示でき、確認再生と位置表示が対応する |
| AC-08 | **第三者AppView再現性** | Musicaのソースを一切使わない独立スクリプトが、公開レコードとblobのみを取得して同じ作品を描画できる |
| AC-09 | 外部ソフトとの往復 | 一般的な楽譜ソフトが書き出した原本を取り込め、取得した原本を同ソフトで開ける |
| AC-10 | **記譜通りの発音** | 任意の構成音・声部数・音域の和音が、書かれた通りの音高で鳴る。Musicaが構成音を置換・省略・追加しない |
| AC-11 | **コードシンボルを鳴らさない** | コードシンボルのみで音符のない箇所が無音である。伴奏・ベース・内声が自動生成されない |
| AC-12 | ABCの往復 | ABCで投稿した作品を取得すると同じABCが返り、原本のMusicXMLと内容が一致する |
| AC-13 | 編集用ソースの縮退 | `editSource` が無い・破損・非対応・未取得でも、`source` から通常どおり表示・再生できる |
| AC-14 | 不正XMLの拒否 | `DOCTYPE`・外部実体・実体展開爆弾・過大ネストを検出して拒否し、外部参照を取得しない |
| AC-15 | 圧縮原本の安全な展開 | パストラバーサル、過大展開、過大エントリ数の `.mxl` を拒否する |
| AC-16 | 表示テキストの無害化 | 原本のタイトル・歌詞・コードシンボルにマークアップを埋め込んでも、画面・SVGの双方で実行・解釈されない |
| AC-17 | 対応外記譜の縮退 | 未対応要素を含む原本でも取り込め、未対応である旨を表示したうえで可能な範囲を描画する |
| AC-18 | 未ログインでの試用 | ログインせずに貼り付け・描画・確認再生ができ、公開操作でのみログインを求められる |
| AC-19 | 通信失敗と容量超過 | 下書きを保持し、重複作品を作らず再試行できる |
| AC-20 | 競合更新 | 変更前CIDの不一致を検出し、原本を保持する |
| AC-21 | iPad対応 | Safariでログイン、ファイル取り込み、貼り付け、譜面表示、確認再生、原本取得ができる |
| AC-22 | 共有と削除 | 本人操作時だけBlueskyに投稿。削除時にMusica表示・キャッシュを失効する |
| AC-23 | 下書き・所有者の継続性 | 下書きは公開送信されず復元可能。ハンドル変更・データ移行済みPDS移行後もDIDで作品を取得できる |
| AC-24 | エージェント経路（第3段階） | MCPサーバ経由で検証・描画・構造要約が行え、`check` の応答から誤りを特定して修正できる |
| AC-25 | 他者コードの隔離（第4段階） | 生成器からトークン、Cookie、親DOM、下書き、外部通信へ到達できず、無限ループを停止できる |

AC-08は設計原則2に、AC-10とAC-11は設計原則4に、AC-13は設計原則3に対応する。これらが通らない設計変更は採用しない。

AC-24は第3段階、AC-25は第4段階の条件であり、初期版のリリース判定には含めない。

## 15. 非機能要件

### 軽量性

ホーム・一覧では描画エンジン、フォント、音源、変換器をロードしない。一覧表示はレコードの要約フィールドのみで成立させ、原本blobを取得しない。取り込み・作品表示時に描画エンジンとフォントをロードし、音源は再生操作時、ABC変換器はABC判別時に取得する。認証SDKも必要な経路でロードする。CDN利用だけでブラウザの取得量が減るとは評価しない。

描画エンジンのロード量は第1段階で実測する。WebAssemblyを採用する場合の初回取得量が対象環境で許容できない場合は、§7の代替候補へ切り替える。

キャッシュキーには原本CID、描画エンジン版、表示条件を含める。キャッシュは派生物として扱い、原本の唯一の保存場所にしない。

### 上限と応答

初期目標は非圧縮原本5 MiB以下、圧縮原本1 MiB以下、展開後20 MiB以下、編集用ソース1 MiB以下、描画10秒以内、最大100ページ、演奏時間30分以下とする。すべてMusica独自の提案上限であり、PDSの保証値ではない。技術検証で対象端末の性能と実際の容量制限に合わせて確定する。

2ペイン画面の再描画は入力停止後に遅延実行し、入力操作を妨げないこと。解析・変換・展開・描画のいずれも、上限超過時に途中で中止できることを条件とする。メモリの厳密な制限はブラウザAPIだけで保証できないため、入力・展開・出力の各段階の上限と中止試験を組み合わせる。

### 利用環境と保存

PC主要ブラウザとiPad Safariを対象とし、キーボード操作・フォーカス表示・作品の代替説明を提供する。端末内下書きは自動的な他端末同期や永久保存を保証せず、書き出しを提供する。

### 配信と公開情報

原本配信プロキシは任意URL取得APIにしない。DID・公開レコード・CIDの関連を確認し、内部ネットワーク、リダイレクト、取得時間、取得サイズを制限する。

公開原本には認証情報・秘密鍵・個人用APIキーを含めない。権利情報や作曲者と投稿者の区別を保持する。他者の著作物を投稿する場合の責任は投稿者にあることを表示する。Musica上の表示除外と本人によるPDSレコード削除を区別する。

## 16. 実装順序と未確定事項

単一障害点を最初に置かない。認証より前に自己利用できる状態を作り、他者コードの実行は最後に置く。

### 第1段階：貼る→鳴る（認証なし）

取り込み画面の2ペイン、ファイル取り込みとテキスト貼り付け、形式判別、§6の検証と拒否条件、描画エンジンの選定と適用、Bravuraの適用、確認再生とハイライト、ABC→MusicXML変換の検証。悪意ある原本の試験データ一式をここで用意する。

**この段階の完了時点で、作者自身が毎日使える状態になる。** ここを短期で通すことを最優先とする。

### 第2段階：複数ユーザー公開

OAuth、本人のPDSへのblobとレコードの保存・取得・更新・削除、作者一覧、共有URL、Bluesky共有、下書き。別アカウント2件で確認する。実際の投稿試験は許可されたテストアカウントで行う。**AC-01〜23を満たして初期版の公開判定とする。**

### 第3段階：エージェント向けインターフェース

MCPサーバ、`check` の構造要約、`render`、`play`、公開操作。CLIはその後。**第2段階と並行または先行してよい。** 優先度は作者自身の使い方で決める。

### 第4段階：他者コードの閲覧時実行

隔離実行環境、生成器のアップロードとアプリ上での実行、出力の検証と更新操作。隔離が未検証の状態で任意ユーザーのコード実行を公開しない。**この段階が完了しなくても製品は完結する。**

| 未確定項目 | 決定条件 |
| --- | --- |
| ドメイン・正式NSID | 長期保持できるドメインから命名。公開後に変更しない |
| 描画エンジン | 第1段階で品質・ロード量・ライセンス・iPad動作を実測して確定 |
| ライセンス適合 | 採用エンジンの配布条件がMusicaの公開形態と両立するか確認 |
| ABC受理の可否 | ABC→MusicXML変換の再現性・欠落・エラー報告が実用水準か第1段階で判定 |
| 原本のMIME type | 対象PDSが受理する型とLexiconを整合させる |
| 圧縮原本の受理 | `.mxl` を初期版で受理するか、非圧縮のみに限定するか |
| 音声実装 | ブラウザ再生ライブラリと配布可能なピアノ音源を選定 |
| `check` の項目 | 実際にエージェントに使わせ、自己修正に足りる情報を特定 |
| ホスティング | Astro配信と原本配信を配置できる環境 |
| 性能・容量 | 実測とPDS制限で提案値を調整 |

MEI等の追加受理形式、演奏表現データ、全体検索、複数音色、MIDI書き出し、版履歴は後続候補とする。**MusicXML原本・blob保存・Astro・全利用者の本人名義公開・第三者AppViewでの再現性・書かれていない音を鳴らさないことは確定方針とする。**

## 17. 参照資料

設計・API・上限値はMusicaの提案であり、外部ライブラリの既存機能を保証する記述ではない。

- [Astro](https://docs.astro.build/en/concepts/why-astro/)
- [AT Protocol OAuth](https://atproto.com/guides/about-oauth)
- [Lexicon](https://atproto.com/specs/lexicon)
- [Blobs](https://atproto.com/specs/blob)
- [Verovio](https://github.com/rism-digital/verovio)
- [Verovio: SMuFLフォント（Bravura同梱・`font` オプション）](https://book.verovio.org/advanced-topics/smufl.html)
- [Verovio: MIDI再生とハイライト](https://book.verovio.org/interactive-notation/playing-midi.html)
- [Verovio: 出力形式（MusicXML出力は非対応）](https://book.verovio.org/toolkit-reference/output-formats.html)
- [OpenSheetMusicDisplay](https://github.com/opensheetmusicdisplay/opensheetmusicdisplay)
- [VexFlow](https://github.com/vexflow/vexflow)
- [Bravura](https://github.com/steinbergmedia/bravura)
- [MusicXML 4.0](https://www.w3.org/2021/06/musicxml40/)
- [ABC notation: コードシンボルと和音](https://trillian.mit.edu/~jc/music/abc/doc/ABCtut_Chords.html)
- [abc2xml（Wim Vree）](https://wim.vree.org/svgParse/abc2xml.html)
